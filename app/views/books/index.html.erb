<h2>Book</h2>

<table>
  <thead>
    <tr>
      <th>title</th>
      <th>Opinion</th>
      <th>colspan="3"</th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
    <tr>
      # 作成途中<td><%= image_tag post_image.user.get_profile_image(100,100)</td>
      <td><%= book.title %></td>
      <td><%= book.opinion %></td>
    </tr>
  </tbody>

</table>
